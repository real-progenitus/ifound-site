import MapView from '@/app/components/map/MapView';

export const metadata = {
  title: 'iFound - Map',
  description: 'Explore lost and found items on the interactive map.',
};

export default function MapPage() {
  return <MapView />;
}
